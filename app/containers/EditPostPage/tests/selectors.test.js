// import { fromJS } from 'immutable';
// import { makeSelectEditPostPageDomain } from '../selectors';

// const selector = makeSelectEditPostPageDomain();

describe('makeSelectEditPostPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
