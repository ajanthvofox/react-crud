// import { fromJS } from 'immutable';
// import { makeSelectSinglePostPageDomain } from '../selectors';

// const selector = makeSelectSinglePostPageDomain();

describe('makeSelectSinglePostPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
