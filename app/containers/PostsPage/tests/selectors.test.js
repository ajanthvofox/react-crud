// import { fromJS } from 'immutable';
// import { makeSelectPostsPageDomain } from '../selectors';

// const selector = makeSelectPostsPageDomain();

describe('makeSelectPostsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
